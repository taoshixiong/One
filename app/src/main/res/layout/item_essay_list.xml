<?xml version="1.0" encoding="utf-8"?>
<android.support.v7.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:card="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="90dp"
    android:clickable="true"
    android:descendantFocusability="blocksDescendants"
    android:focusable="true"
    android:focusableInTouchMode="false"
    android:foreground="?android:attr/selectableItemBackground"
    card:cardBackgroundColor="@android:color/white"
    card:cardCornerRadius="1dp"
    card:cardElevation="1dp"
    card:cardMaxElevation="1dp">
    <!--cardMaxElevation：最大卡片阴影的宽度-->
    <!--cardElevation:卡片阴影的宽度-->
    <!--cardBackgroundColor:卡片的背景颜色-->
    <!--cardCornerRadius ：卡片的圆角半径-->

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:padding="4dp"
        android:orientation="vertical">

        <TextView
            android:id="@+id/item_tv_title"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_alignParentBottom="true"
            android:layout_alignParentEnd="true"
            android:gravity="center_vertical"
            android:padding="@dimen/recycler_item_pading"
            android:text="Test"
            android:textColor="@android:color/black"
            android:maxLines="1"
            android:textSize="20dp" />

        <TextView
        android:id="@+id/item_tv_sub_title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_alignParentEnd="true"
        android:layout_weight="1"
        android:maxLines="2"
        android:paddingLeft="@dimen/recycler_item_pading"
        android:paddingRight="@dimen/recycler_item_pading"
        android:text="Test"
        android:textSize="16dp" />
    </LinearLayout>


</android.support.v7.widget.CardView>
